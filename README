# Project Build & Run Guide

This guide shows how to format, build, and run the project on Windows and macOS using vcpkg and CMake. The exact commands remain unchanged; we've simply clarified the steps and context.

## Code Formatting
# Format code
clang-format.exe -i src/*.h src/*.cpp

## Windows: Build
set VCPKG_ROOT="$HOME/vcpkg"
vcpkg install
vcpkg integrate install
cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE="${VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake"
cmake --build build

## macOS: Build
brew install vcpkg
brew install pkg-config
git clone https://github.com/microsoft/vcpkg "$HOME/vcpkg"
export VCPKG_ROOT="$HOME/vcpkg"
vcpkg install
vcpkg integrate install
cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE="${VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake"
cmake --build build

## Debug Build
cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE="${VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake" -DCMAKE_BUILD_TYPE=Debug
cmake --build build

## Release Build
cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE="${VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake" -DCMAKE_BUILD_TYPE=Release
cmake --build build

## Run (after a successful build)

```bash
cd build
./helloworld
```


cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_FLAGS="-g -O3 -fno-omit-frame-pointer"
cmake --build build 